#include<stdio.h>

void main()
{
	int x1, x2, y1, y2, r1, r2;
	printf("¬ведите координаты и радиус превой окружности");
	scanf("%d%d%d", &x1, &y1, &r1);
	printf("¬ведите координаты и радиус превой окружности");
	scanf("%d%d%d", &x2, &y2, r2);
	if (x1 == x2 && y1 == y2 && r1 > r2)
	{
		printf("¬тора€ окружность находитс€ в первой");
	}
	if (x1 == x2 && y1 == y2 && r1 == r2)
	{
		printf("ќкружности равны");
	}


}